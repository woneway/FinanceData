<!-- 用法：复制本文件到 changes/<slug>/acceptance.md 后填写实际内容。请保留全部 H2 章节，不允许删除或重命名。 -->

## 验收结论

<!-- 一段话总结本 change 是否完成、是否可归档。例：「变更 X 已完成实现和验证。<能力 Y> 已落地，<入口 A/B/C> 已切换到新契约。」 -->

## Completeness

<!-- 列出本 change 在 spec 中要求的每条 Requirement 是否已实现，给出代码或配置层面的证据。每条用 `-` 列点。 -->

- 已实现 <Requirement 1>：<证据，例如新增/修改的函数、配置、文件路径>。
- 已实现 <Requirement 2>：<证据>。

## Correctness

<!-- 列出实际执行的测试命令清单和验证结果，便于后续复现。Playwright / 手工验证的场景也写在此处。 -->

已执行并通过：

- `<测试命令 1，例如 uv run pytest tests/xxx.py>`
- `<测试命令 2>`

Playwright 已验证（仅当涉及前端）：

- <场景描述>

## Coherence

<!-- 说明本次变更与既有架构契约的一致性：source-of-truth 层级是否仍然成立、是否有绕过 service 直调 provider 的捷径、命名是否符合规范、文档是否同步。 -->

- <一致性陈述 1，例如「ToolSpec 继续作为工具契约真相源」>
- <一致性陈述 2>

## 未测试项与风险

<!-- 显式列出未覆盖的场景与对应风险。即使为空也写「无」。 -->

- <未测试场景 1 与风险评估>
- 无 / 或 <说明>

## Spec Drift

<!-- 显式列出 spec 与实现的偏离项，给出处理结论（已同步 spec / 已修代码 / 待跟进）。即使为空也写「无」。 -->

- 无 / 或 <drift 描述与处理>

## 上游未对齐项

<!-- 涉及上游金融数据源时，显式列出未与官方文档对齐的字段、单位、更新时间或更新策略。即使为空也写「无」。 -->

- 无 / 或 <未对齐项与跟进计划>
