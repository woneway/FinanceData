<!-- 用法：复制本文件到 changes/<slug>/upstream-alignment.md 后填写实际内容。请保留全部 H2 章节并按需新增数据维度小节。-->

# 上游对齐记录

<!--
本文件用于记录本 change 涉及的上游金融数据接口与项目内 provider 适配层的字段、单位、调用约定的对齐结论。
每个数据维度（如日线、周线、北向资金、龙虎榜）独立成一个 H2 小节，并在小节内用对照表覆盖以下七个维度：
1. 调用方式（含函数签名 / endpoint / 必填参数）
2. 字段（请直接打印 columns 列出，不要凭文档猜测）
3. 单位（特别注意手 vs 股、千元 vs 元、% vs 小数）
4. 复权策略（前复权 / 后复权 / 不复权 的参数名）
5. 历史范围（起始日期）
6. 更新时间（T+1、T+0、收盘后、盘中实时）
7. 状态（✅ 稳定 / ⚠️ 待验证 / ❌ 不可用 / 🟡 需代理绕过）

每个 H2 小节末尾给出该维度的「主源 + fallback」结论。

文件最末必须有 H2「结论」总结全文：源选择、单位换算策略、是否需要代理绕过、本 change 不覆盖的范围。
-->

## <数据维度 1，例如：日线 (daily)>

| 维度 | <主源，例如：tushare (doc_id=27)> | <fallback，例如：akshare (stock_zh_a_hist_em)> |
|------|-----------------------------------|------------------------------------------------|
| 调用方式 | `<函数签名 / 必填参数>` | `<函数签名 / 必填参数>` |
| 字段 | `<打印 df.columns 实际列名>` | `<打印 df.columns 实际列名>` |
| 单位 | <vol/amount/pct_chg 的实际单位> | <vol/amount/pct_chg 的实际单位> |
| 复权 | <参数名与可选值> | <参数名与可选值> |
| 历史范围 | <起始日期> | <起始日期> |
| 更新时间 | <T+1 / T+0 / 收盘后 / 盘中实时> | <T+1 / T+0 / 收盘后 / 盘中实时> |
| 状态 | ✅ / ⚠️ / ❌ / 🟡 | ✅ / ⚠️ / ❌ / 🟡 |

**结论**：<主源 + fallback 的选择与原因>

## <数据维度 2，例如：周线 (weekly)>

<!-- 同上结构 -->

## 结论

<!--
全文总结。建议按以下条目逐项写：
1. 各数据维度的主源 + fallback 选择
2. 单位统一策略（哪些字段需要在 provider 适配层做 ×100 / ×1000 / ÷100 等转换）
3. 是否需要 ensure_eastmoney_no_proxy() 代理绕过，覆盖哪些 provider 文件
4. 本 change 不覆盖的上游能力（已知但暂不接入），并给出原因
-->

1. **<维度 1>**: <主源> + <fallback>，<状态说明>
2. **<维度 2>**: <主源> + <fallback>，<状态说明>
3. **单位统一**: <换算规则>
4. **代理绕过**: <是否需要 / 覆盖范围>
5. **不覆盖范围**: <说明>
